using System.Collections;
using System.Collections.Generic;

namespace Zilla.Xtructs
{
    public class UsePriorityList<T>
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
